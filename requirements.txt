general architecture of qkd networks.

schematic representation of the simulated setup for implementing a prepare and measure discrete variable qkd protocol . this figure illustrates all the components involved in the quantum communication process most of which are implemented using the python library . both nodes are equipped with a quantum processor for qubit state manipulation. the sender node includes a qubit source functioning as a single photon source while the receiver node features a quantum measurement unit quantum detector acting as a single photon detector.

time space diagram showing the emission reception and measurement of a single photon used to determine the repetition time for alices sps in the bb84 protocol. violet segments represent the time needed to manipulate the emitted photons state gray dashed areas represent the channels timing jitter green segments indicate the time windows where of detector activation and the yellow area shows the time from photon arrival to detection. the diagram provides an appropriate value for which is not the minimum for correct synchronization but is defined based on the actual lower bound. the relative sizes of the intervals are not to scale.

optimal extra noise as a function of link distance and output key length . the three subplots correspond to each strategy with shown at the top in the middle and at the bottom. the values are obtained by numerically solving eq. using the parameters in tab. . the red dashed line indicates the limit distance associated with the security threshold .

behavior of our simulation setup compared with expected mean values lines and standard deviations dashed areas . the simulation has been executed considering the parameters in tab selecting strategy for parameter estimation as the number of initial photons and as the artificial flip probability. each experimental point and its error bar has been obtained running our bb84 variant times. to validate the simulation results with the theoretical framework explained throughout this paper we have used the second row of tab. and eqs. and to calculate the expected value and standard deviation of the qber right axis eq. to estimate the left axis and eq. for the protocol success probability right axis . as red dashed lines we also plot the boundary horizontal and the limit distance vertical corresponding to such threshold. this is also the distance for which goes to 0.

representation of for two output length specifications bits top and bits bottom . they have been calculated using eqs. and for each parameter estimation strategy in tab. . each one of these three series also show the calculation corresponding to the case of where bob does not add extra noise to the raw key. the violet dashed line shows the bare minimum number of photons computed for and without considering restriction .

performance of our reverse analysis fixing as the required output key length and simulating the qkd protocol using . output key length results are shown in subfigures a and b for and respectively. the violet series represents the case of the bare minimum number of photons computed for strategy without applying restriction . for each link distance we perform protocol simulations to obtain the mean output and its standard deviation represented as error bars. subfigures c and d show the simulated protocol duration accounting for both the quantum transmission and classical post processing stages. subfigures e and f present the corresponding kbr in bits/s. in all subplots the limit distance is marked as a vertical red dashed line. the three parameter estimation strategies are clearly distinguished using the same color scheme as in <Reference>. all simulations were carried out using the parameters listed in table .

